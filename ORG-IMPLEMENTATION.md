# Организация ИИ-сотрудников — статус реализации

План: `~/.gstack/projects/petable/egorproskurin-worktree-effervescent-squishing-valley-design-20260730-105840.md` (Rev 7, ENG+DESIGN CLEARED). Проверка всего: `swift test` (304+ зелёных) и `make build`.

## Сделано (2026-07-30)

- **Слайс 0** — спайк CLI: фикстуры `Tests/AgentRuntimeTests/Fixtures/`; находки: codex требует `--skip-git-repo-check` и строгую `--output-schema` (все properties в required); claude — закрытый stdin; usage/session_id в `result`.
- **Слайс 1** — `Sources/GraphCore/Organization.swift` (модель + валидатор T1), `OrganizationPresets.swift` (7 пресетов + `makeDefault()`), конверт **v14** (`organization:`), тесты `OrganizationTests`.
- **Слайс 1б** — `Sources/OrgEngine/`: `Run.swift` (OrganizationRun, снапшот T4), `EventStore.swift` (JSONL, карантин 3A: битый хвост отброшен / середина = broken; интенты T2; орфан-PID; `summaries`), реконсиляция `Organization.reconcile` (в GraphCore).
- **Слайс 2** — `AgentRuntime`: `AgentEvent/Verdict` (T5-парсер), `StreamParsers` (claude/codex на фикстурах), `CLIInvocation`, `CLIProcessAdapter` (общий запускатель, 4A), `CLIDiscovery` (env-PATH → login-zsh, кэш; им пользуется и `AgentService` приложения); `OrgEngine`: `Engine.swift` (чистая машина: единый счётчик возвратов, «ближайший work вверх по пути», вход в merge = гейт, аннулирование Approve, recovery), `StageRunner` (оркестрация этапа + слепки), `Wire.swift` (Codable-over-Data 1A), `DaemonCore` (актор: команды → конвейер; e2e-тесты на FakeAdapter), `XPCBridge.swift` (@objc Data-граница, `petableDaemonMachService`).
- **Слайс 3** — `WorktreeManager.swift` на живом git: ветка `org/<key>-<runID6>`, ref-пин `refs/petable/<runID>`, harness через **pathspec-исключение** (`:(exclude)…` — git читает info/exclude из COMMON dir, per-worktree НЕ работает: T7.1 дока поправлен), commit артефакта, reset при recovery, rebase→(retest)→merge, конфликт-abort. `Sources/petable-daemon/main.swift` — executable (LaunchAgent-скелет П0).
- **Слайс 4 (начат)** — документ: `organization` load/save, `createOrganizationIfNeeded`, `updateOrganization`, `reconcileRunSummaries`; сайдбар: секция «Организация»; `App/Sources/OrganizationView.swift` v1 (пустое состояние, флоу приглушёнными контурами, борд, история). Приложение линкует AgentRuntime (project.yml + xcodegen).

## Сделано дополнительно (эта же сессия, продолжение)

- **Живой конвейер в приложении**: `Sources/OrgEngine/EngineTransport.swift` (протокол; `InProcessTransport` поверх DaemonCore; `XPCTransport`-клиент с handshake-таймаутом 800 мс и fallback), `App/Sources/OrganizationController.swift` (@MainActor: runs, mode-баннер, start/approve/reject/chat/cancel, реконсиляция саммари в документ + снятие задачи с борда), живые строки задач в `OrganizationView` (статус по словарю 8A, гейт Принять/Вернуть, чат-поле только в {ждёт гейта, требует внимания}, disabled-подпись «чат откроется на гейте» — 7A).
- **Зов конвейера (5A)**: UNUserNotificationCenter-уведомления на {ждёт решения, требует внимания, смёржено} + Dock-бейдж = числу ждущих (в контроллере).
- **Слайс 8 — декомпозиция**: `RunStatus.waitingChildren`, `Engine.decomposed/childrenSettled`, `StageRunner.StageResult` (вердикт наружу), `DaemonCore.spawnChildren` (резолвинг типов/репо по именам, jiraKey `DN-9.N`, дети последовательно; отмена/сбой ребёнка будит родителя), тесты `DecomposeTests` (fan-out 2 подзадачи по разным репо → гейты детей → родитель merged; пустая декомпозиция; отмена ребёнка).

- **Слайс 7 — Jira туда-обратно (П4″)**: `Sources/OrgEngine/Jira.swift` — `JiraConfig`/`JiraIssue`/`JiraError`, `JiraHTTPTransport`-шов, `JiraClient` (REST v2: search/comment/hasComment/transitionToDone — переход выбирается по statusCategory=done, отсутствие = no-op), `JiraGateway`-протокол для DaemonCore, `JiraImporter.map` (чистый маппинг: jiraType → имя → первый; проект → репо; непривязанный проект = пропуск с причиной; дедуп по jiraKey). Wire: тип `configure` + `ConfigureCommand` (секреты демону ТОЛЬКО в память). DaemonCore: write-back при merged по intent-протоколу T2 (`jira:<runID>`, маркер `[petable:<runID>]` в комментарии, тело в intent-событии), очередь `pendingJira` без секретов/при отказе, recovery подхватывает незакрытые jira-интенты и сверяет факт по маркеру — дубль невозможен; дети декомпозиции (`DN-9.N`) пропускаются. WorktreeManager: `pushAndCommitURL` (push default+ветки в origin; ссылка деградирует до хеша — T7.4), `diffStat`. Приложение: `JiraSettingsStore` (baseURL/email — UserDefaults, токен — Keychain), `OrganizationController.importFromJira/configureJira` (configure при коннекте и после импорта; 401 → `jiraNeedsReauth`), борд: кнопка «Импорт из Jira» + шит настроек + баннер статуса/переавторизации; секция «РЕПОЗИТОРИИ» (минимум реестра: путь/Jira-проект/тест-команда, правки по ⏎). Тесты `JiraTests` (15): клиент на фикстурах, маппинг, write-back e2e, очередь, retry, recovery-сверка, commitURL.

- **Канвас: живой токен + фокус-режим + клавиатура** (`OrganizationView`): токен-слой над узлами флоу (позиция = currentStage, геометрия строчная: центр этапа i = i·124+48; переход — .position с ease-out 220 мс, Reduce Motion — мгновенно, 9A); перегруженный этап — стек «N задач» с циклическим кликом (1A); возврат распознаётся по движению индекса назад (`trackReturns`) → янтарная вспышка узла-получателя 450 мс; фокус-режим (клик по токену ИЛИ задаче борда — D3, рендер по СНАПШОТУ run.flow, панель статуса + гейт/чат, Esc — обзор); клавиатура 11A: N/P — очередь внимания (`controller.attentionRuns` по кругу), ⌘Enter — Принять только на сфокусированном гейте, двухшаговое подтверждение (armed 3 с).
- **Слайс 9 — редактор флоу v1**: GraphCore `OrgFlow.insertStage(after:)/removeStage` (перешивка рёбер линейной цепочки), `FlowIssue.stageID` + `OrgFlow.describe(_:)` (словарь 8A); тесты `FlowEditingTests` (5). UI: тумблер «Наблюдение / Редактор флоу» (2A, в редакторе токенов нет); палитра видов — меню «+» между узлами; инспектор этапа (имя/вид/гейт/сотрудник/промпт, текст по ⏎, пикеры сразу); issues-навигатор («N ошибок флоу», клик подсвечивает узел + открывает инспектор); компактная таблица «ТИПЫ ЗАДАЧ → ФЛОУ» (имя, jiraType — маппинг слайса 7, пикер флоу, добавление/удаление; новый тип автоматически роутится на первый флоу); «Запустить» disabled с точной причиной в строке задачи (нет типа / нет маршрута / N ошибок флоу / нет репозитория).

## Правки автора (2026-07-30, вечер) — реализовано

1. **Разделение экранов (№1/№4)**: сайдбар-группа «Организация» → два пункта. «Конвейер» = `App/Sources/PipelineView.swift`: список задач по статусам (ТРЕБУЕТ ВНИМАНИЯ / ЖДЁТ РЕШЕНИЯ / В РАБОТЕ / В ОЧЕРЕДИ / ЗАВЕРШЁННЫЕ), импорт из Jira, добавление задач. «Организация» = `OrganizationView.swift` (переписан): сегменты «Типы задач · Сотрудники · Конвейер · Интеграции». Общий словарь/узлы — `App/Sources/OrgShared.swift` (`OrgUI`, `StageNodeView`, `StageEmphasis`). Контроллер ОДИН на документ: `PetableDocument.organizationController` (общий транспорт/оркестратор), `ResearchSelection.organizationSettings` + `SidebarItem.organizationSettings`.
2. **Клик по задаче → флоу (№2)**: деталь задачи в PipelineView — диаграмма пути (пройденные — зелёные галки, текущий — токен+акцент, впереди — приглушены; снапшот run.flow), Esc/«‹ Задачи» назад; N/P/⌘Enter (двухшаговый) сохранены.
3. **Клик по этапу → чат (№3)**: панель этапа (сотрудник, статус); чат активен в {ждёт гейта, требует внимания} — «Комментарий или правка поведения», перезапуск этапа с контекстом (границы П9); running — заблокирован с причиной (7A); пройден — read-only подпись.
4. **Интеграции (№5)**: карточки Jira (подключить/настроить + статус), GitHub (`GitHubSettingsStore`: токен Keychain + логин; `Sources/OrgEngine/GitHub.swift`: `GitHubClient.viewerLogin/createRepo`, `RepoProvisioner.clone` в `…/Petable/repos`), Репозитории (реестр + локальный путь + клон по URL + создание приватного в GitHub с автоклоном), Оркестратор (настройки).
5. **Сотрудники (№6)**: master-detail в «Организации»: список + `EmployeeEditor` (имя, ролевой промпт, cli, модель, права; «из пресета», «Сбросить к пресету», удалить), добавление из 7 пресетов или с нуля.
6. **Jira-статусы по этапам (№7)**: `JiraGateway.transitionBestMatch(hints:category:)` (имя приоритетнее категории; нет похожего — no-op), `DaemonCore.syncJiraStage` на переходах (дедуп `jiraStageSync`; hints: work/decompose/join→in progress, review/merge-гейт→review, test→test/qa); финальный Done — по-прежнему intent-очередью. `transitionToDone` = bestMatch(category: done).
7. **Оркестратор (№8/№9)**: `OrchestratorSettings` (enabled/интервал 1-5-15-60 мин/autoStart) + цикл в `OrganizationController.restartOrchestrator`: тик = импорт новых из Jira → нотификация «берём в работу?» ИЛИ автозапуск (`autoStart`), зависшие (needsAttention/waitingGate) — одно напоминание на состояние (сброс при выходе из статуса).

8. **Персист организации (правка «не сохраняется при перезапуске»)**: корень — `updateOrganization` не регистрировал undo, ReferenceFileDocument не считал документ изменённым и автосейв не писал; вдобавок `attach(undoManager)` вызывал только CanvasRootView. Теперь: `PetableDocument.updateOrganization/createOrganizationIfNeeded` регистрируют undo (`registerOrganizationUndo`, снапшот всей организации — заодно ⌘Z по правкам), а PipelineView и OrganizationView делают `document.attach(undoManager)` в onAppear/onChange. `reconcileRunSummaries` — по-прежнему вне undo (П1′): идемпотентно восстановится при следующем коннекте.
9. **Конструктор флоу в «Типах задач» (правка №2)**: у строки типа — кнопка «Флоу» (chevron), раскрывающая редактор смаршрутизированного флоу прямо под типом (тот же editorFlowSection: «+»-палитра, инспектор, issues-навигатор); маршрута нет — «Создать флоу для „типа“» (новый флоу имени типа: один Merge-гейт, этапы достраиваются «+»; пример из правки: discovery → арх-discovery → разработка → ревью → тесты → merge собирается пресетами продакт-менеджер/архитектор/разработчик).

10. **Самообновление приложения (правка автора)**: `App/Sources/UpdateService.swift` + `UpdateBannerView` (плашка над контентом окна, PetableApp) + скрипт-фаза в project.yml (postBuildScripts «Embed build SHA»: пишет `PetableBuildSHA`/`PetableRepoPath` в Info.plist продукта ДО подписи — проверено: codesign valid). Логика: раз в час `git fetch origin main` в репо сборки → origin/main != buildSHA и buildSHA — предок (разошедшиеся ветки не дёргают) → плашка «Доступно обновление · N коммитов» + нотификация (раз на SHA); «Обновить»: клон `--shared --no-checkout` → detached checkout origin/main → `make build` в staging (`…/Petable/update/src`) → подмена .app с бэкапом/откатом → relaunch (`sleep; open` + terminate); «Позже» глушит до следующего SHA. Ограничение: работает на машине с репо и toolchain (xcodegen/xcodebuild) — это и есть модель дистрибуции сейчас; при переходе на Developer ID + релизы (TODOS) заменить на Sparkle/GitHub Releases.

11. **Jira-коннектор в один клик (правка автора)**: OAuth 2.0 (3LO) Atlassian — `App/Sources/JiraOAuth.swift`: `JiraOAuthFlow.connect()` (браузер → согласие → localhost-листенер NWListener на 52786 → обмен кода → сайт сам из accessible-resources), rotating refresh (`ensureFreshJira` с запасом 5 мин перед configure/импортом; истечение у демона переживается очередью write-back), токены в Keychain (`JiraOAuthTokenStore`), общий `KeychainItem`. `JiraConfig` расширен `bearerToken` (приоритетнее Basic; база `api.atlassian.com/ex/jira/<cloudId>`) — обратная совместимость Wire сохранена. UI: «Подключить Jira» (prominent) + «жду подтверждения в браузере…», «Вручную (API-токен)…» — fallback; однократная настройка коннектора (client_id/secret своего OAuth-приложения developer.atlassian.com, callback `http://localhost:52786/callback`) — Atlassian без зарегистрированного приложения OAuth не даёт, это неустранимый шаг. ⚠️ Scopes classic (`read:jira-work write:jira-work offline_access`) — при создании OAuth-приложения включить Jira API разрешения ровно под них.

12. **Репозиторий выбирает агент (правка автора)**: маппинг `RepoRef.jiraProject` больше НЕ обязателен. Импорт: пропуск только при пустом реестре; непривязанный проект → единственный репозиторий берётся сам, при нескольких `repoID = nil`. Старт: `DaemonCore.resolveRepoIfNeeded` — один репо без LLM; несколько → короткий вызов адаптера (промпт со списком реестра, ответ `{"status":"done","note":"<имя>"}`, матч по имени; нет адаптера/не распознан → первый). JQL импорта: без привязок тянутся ВСЕ открытые задачи сайта. `startBlockReason`: блок репозитория только при пустом реестре. После OAuth-подключения импорт запускается сразу сам. Тесты: `RepoResolutionTests` (2), импортёр обновлён.

13. **Нулевая настройка (правка автора «ничего не привязывать»)**: импорт Jira — БЕЗ условий (пустой реестр не блокирует; JQL — все нетерминальные задачи сайта). Старт при пустом реестре: приложение шлёт `StartRunCommand.candidates` (список GitHub-аккаунта: `GitHubClient.listRepos`, имя+ssh+https), демон `resolveRepoIfNeeded/pickRepoName` LLM-вызовом выбирает кандидата по тексту задачи, `cloneCandidate` клонирует в `<store>/repos/<имя>` (идемпотентно; ssh → https), репо попадает в снапшот запуска; контроллер, увидев незнакомый `run.repo` в runState, дописывает его в реестр документа. `startBlockReason`: блок только «ни реестра, ни GitHub». Тесты: cloneFromCandidate (живой git), импортёр при пустом реестре.

14. **Установка демона из приложения + drain (П0, остаток)**: бинарь демона встраивается в ресурсы бандла (project.yml «Embed daemon»: вложенный `swift build` через `env -i`, `ENABLE_USER_SCRIPT_SANDBOXING: false`); `App/Sources/DaemonManager.swift` (install = bootout → копия → plist → bootstrap; `updateAvailable` = сравнение содержимого бинарей); Wire: команда `drain` + событие `drained`; DaemonCore: `draining/activeStageCount` (гейт цикла ПЕРЕД новым этапом, чат тоже), `publishDrainedIfIdle`, `recoverAll()` (все orgID из `EventStore.listOrgIDs()` + доводка `.running`-запусков) — демон-executable теперь вызывает recovery при старте (раньше НЕ вызывал — дыра); контроллер: `installOrUpdateEngine` (drain-ожидание c continuation, таймаут 600 с, реконнект), `engineUpdateAvailable/engineStatus`; UI: `EngineBannerView` (OrgShared, в PipelineView) + карточка «ДВИЖОК» в Интеграциях. Тест `drainThenRecoverAll`.

15. **Канвас — доделки 6A/2A/5A**: `OrganizationRun` +6 опциональных полей (`lastEventAt/flowVersion/mergeSHA/commitURL/diffStat/pushFailed` — старые журналы читаются); `RunSummary` +3 (`mergeSHA/commitURL/diffStat`); `OrgFlow.version` (custom `init(from:)`, без поля = v1; бамп в `PetableDocument.updateOrganization` при правке stages); DaemonCore: `publishState` штампует lastEventAt, `heartbeat` ≤1 Гц через `StageRunner.onEvent`-колбэк, `subscribe` пушит снапшоты всех запусков (reconnect = снапшот, раньше канвас ждал следующего события — дыра); merge-итог пишется в run. UI (PipelineView): «последнее событие N сек назад» (TimelineView, застой >120 с — янтарный), тихий пульс токена (scaleEffect на смену lastEventAt), бейдж «Запуск идёт по vN · текущий флоу vM», терминальная карточка (итог/время/≈$/возвраты/дифф-стат/ссылки коммит+Jira/отказ push), сводка «пока вас не было: N задач ждут» (controller.awaySummary, гаснет при открытии задачи), ссылка «коммит» в истории. Тест `flowVersionBackcompat`.

16. **Остатки правок автора №5/№7/№8**: (№5) агентные репо в декомпозиции — `ConfigureCommand.githubToken` (демону, только в память; controller.configureJira шлёт Jira+GitHub вместе, connectGitHub переконфигурирует), `DaemonCore.resolveChildRepo` (незнакомое имя репо подзадачи → `GitHubClient.createRepo` + клон в `<store>/repos`, реестр организации демона пополняется, контроллер допишет в документ по run.repo; сбой → репо родителя), инжект `github: GitHubClient` в DaemonCore, промпт decompose говорит «новое имя = будет создан»; (№7) `Organization.jiraStatusMap: [String:String]?` (kind → имя статуса; decompose/join наследуют work, merge — review; `DaemonCore.customStatus`), syncJiraStage ставит кастомное имя ПЕРВЫМ хинтом, UI-редактор в карточке Jira (4 поля, ⏎); (№8) `OrchestratorSettings.resolveStuck` + `OrganizationController.resolveStuckRun` — оркестратор-LLM (claude CLI) разбирает needsAttention: `retry: <совет>` → chat-перезапуск, `cancel` → отмена, `wait`/нераспознано → нотификация; гейты (waitingGate) — всегда человеку; тумблер в карточке Оркестратора. Тесты: `decomposeCreatesRepo` (фейк-GitHub-транспорт + живой git-клон), `customStatusMapSync`, `customStatusInheritance`.

17. **Слайс 10 остаток + 12 (дебаггер v1) + 13 (тени)**: (10) `EmployeeEditor` расширен — effort, лимиты (токены/минуты/чат-итерации, ⏎), allowedTools/skills/harness с пометками непереводимых полей per-CLI (claude: readOnly не транслируется; codex: allowedTools/harness; skills — «пока не транслируются»), «ФАКТИЧЕСКИЙ ВЫЗОВ» = живые `CLIInvocation`-аргументы; (12–13) `OrganizationRun.experimental/shadowOf/forkOf` (+`RunSummary.experimental`), `Engine.approve` на merge для experimental = close (merge недостижим по построению), Wire `shadowRun/forkRun` (+`ShadowRunCommand/ForkRunCommand`), `DaemonCore.startShadow` (маршрут типа на экспериментальный флоу, ветка `org/shadow/`, shadowOf = primary) и `startFork` (продолжение с ТЕКУЩЕГО этапа, worktree от baseSHA источника — `createWorktree(run:from:)`, override модели всех сотрудников, ветка `org/fork/`; на merge-этапе — сразу waitingGate), гарды experimental в syncJiraStage/enqueueJiraWriteBack/continueMergeIfNeeded, `runID(forTask:)`/controller.run(forTask:) предпочитают primary; контроллер: experimental не в attentionRuns/нотификациях, финал тени НЕ снимает задачу с борда; UI: меню «Тень» (пикер флоу) + полупрозрачные строки экспериментов в детали задачи (PipelineView), `App/Sources/DebuggerView.swift` — три зоны 12A (список запусков live+история, флоу-снапшот на позиции scrubber'а по событиям журнала, слайдер+карточка события, словарь 8A), сравнение метрик двух запусков, fork с полем модели; сайдбар-пункт «Дебаггер» (`SidebarItem/ResearchSelection.organizationDebugger`). Тесты: `ShadowForkTests` (2).

18. **Миграция Jira search (2026-07-31)**: живой сайт вернул HTTP 410 — Atlassian удалил `/rest/api/2/search` (CHANGE-2046); `JiraClient.searchIssues` переведён на `/rest/api/2/search/jql` (v2 — описание плоским текстом; `fields` обязателен явно — без него endpoint отдаёт только id). Форма ответа `{"issues":[…]}` та же, пагинация nextPageToken не используется (maxResults 50 хватает).

19. **Статус Jira + ссылка в строках борда (правка автора 2026-07-31)**: `JiraIssue.status` (+`fields=…,status` в запросе), `OrgTask.jiraStatus: String?`, импортёр пишет статус новым задачам, `importFromJira` освежает его у существующих (дедуп по jiraKey статус не трогает); UI `PipelineView.jiraBadge` — капсула статуса + ссылка `browse/<key>` (из `connectedSiteDisplay`) в строке списка и в шапке детали.

20. **Оркестратор — маршрутизатор запуска + фильтры конвейера (правка автора 2026-07-31)**: (1) PipelineView: `filterBar` — поиск по имени/ключу (фильтрует и историю), Picker статуса конвейера (Все/Внимание/Ждёт/В работе/Очередь/Завершённые), Picker Jira-статуса (уникальные значения задач), «Сбросить»; (2+3) запуск ЛЮБОЙ задачи: `OrgUI.startBlockReason` оставил единственный блок «ни репо, ни GitHub»; `DaemonCore.resolveFlowIfNeeded` на старте (ДО Engine.startRun): чинит тип (нет/битый → первый, пустой список пополняется) → валидный маршрут таблицы как есть → иначе LLM-выбор `pickFlowName` из валидных флоу по типу+jiraStatus+инструкциям (выключаемо `flowByLLM`) → валидных нет — `Organization.makeFallbackFlow()` (линейная запаска из пресетов, GraphCore); выбранный маршрут пишется в организацию демона; (4) карточка «Оркестратор»: живая сводка «как работает» по текущим настройкам (маршрутизация/репозиторий/тик Jira/зависшие), тумблер `OrchestratorSettings.flowByLLM`, `OrchestratorSettings.instructions` (TextEditor + Сохранить) — инструкции уходят демону через `ConfigureCommand.orchestratorInstructions/orchestratorFlowByLLM` и добавляются в промпты выбора флоу/репо (демон) и разбора зависших (приложение). Тесты: `orchestratorPicksFlow`, `orchestratorFallbackFlow`.

21. **Ошибка «исполнитель не установлен» + планы устранения + редизайн настроек (правка автора 2026-07-31)**: корень ошибки — реестр адаптеров собирался ОДИН раз на старте процесса (launchd мог поднять демона до появления CLI в PATH); `AdapterRegistry` получил `fallback`-замыкание (промах → `CLIDiscovery.reset()` + повторный поиск) в демоне и in-process движке; демон логирует найденных исполнителей; `DaemonManager.restart()` + `controller.restartEngine()` (bootout+bootstrap / сброс кэша + реконнект). Планы устранения: `RemediationPlan`/`OrgUI.remediation(for:)`/`RemediationView` (OrgShared) — по причине needsAttention карточка с шагами и кнопками («не установлен» → which/установка/перезапуск движка/чат «продолжай»; конфликт rebase; лимит возвратов; вопрос сотрудника; git:; декомпозиция), врезана в панель этапа PipelineView. Редизайн настроек (ui-ux-pro-max): сегмент-контрол → **мастер-детейл** (`HSplitView`): слева List разделов с живыми статусами (Типы задач N · Сотрудники N · Конвейеры N/ошибки · Движок демон/в приложении/нет · Jira · GitHub логин · Репозитории N · Оркестратор вкл/выкл), справа деталь; сверху — **чек-лист готовности** «до первого запуска» (движок/репо/валидный флоу/Jira-опционально, шаги-кнопки, всё готово → одна зелёная строка); сырые ошибки Jira — lineLimit(2)+help. `integrationsSection` удалена, карточки стали деталями разделов.

22. **Стабильная подпись вместо ad-hoc (правка автора 2026-07-31, «пароль при каждом make run»)**: ad-hoc подпись менялась каждую пересборку → Keychain/TCC считали каждую установку новым приложением, «Разрешить всегда» не переживало переустановку. Теперь: `CODE_SIGN_IDENTITY: "Apple Development"` + `CODE_SIGN_STYLE: Manual` + `DEVELOPMENT_TEAM: AL84K6U294` (бесплатный Xcode-сертификат, TeamIdentifier стабилен); демон подписывается той же identity в фазе «Embed daemon» и в `make daemon-install` (ad-hoc fallback без identity). После ПЕРВОГО запуска новой подписи Keychain спросит последний раз — дальше тихо. Это же закрывает часть TODOS «Developer ID»: TCC демона больше не слетает при пересборке (XPC-защита и Keychain-ACL демона — по-прежнему при переходе на Developer ID). Сертификат Apple Development истекает раз в год — Xcode обновит, промпт вернётся один раз.

23. **«Движок не видит claude» + настройки исполнителей движка (правка автора 2026-07-31)**: корень — claude лежит в `~/.local/bin` (в терминале пользователя `claude` — shell-функция cmux-враппера), а launchd-PATH и `.zprofile` этот каталог не содержат: демон находил только codex. `CLIDiscovery`: порядок поиска теперь ручной путь (`setOverride`) → env-PATH → **well-known каталоги** (`~/.local/bin`, `/opt/homebrew/bin`, `/usr/local/bin`, `~/bin`, `~/.claude/local`, `/usr/bin`) → login-zsh; проверено: демон логирует «исполнители: claude, codex». Настройки движка: `EngineSettings` (UserDefaults: путь + модель по умолчанию per-CLI), UI в разделе «Движок» — на каждый CLI строка статуса (зелёный путь/красный «не найден»), поле ручного пути (⏎ → override локально + демону через `ConfigureCommand.cliPaths` + подсказка перезапустить) и поле «модель по умолчанию»; постоянная кнопка «Перезапустить движок». Дефолтные модели применяются на снапшоте старта: `Organization.applyingDefaultModels` (GraphCore, тест) в `startProvisioned`/`shadowRun` — сотрудник со своей моделью сильнее, документ не мутирует.

24. **«Движок зелёный, но задачи падают» (2026-07-31)**: разбор по журналам — падения случились у ПРЕДЫДУЩЕГО процесса демона (старый бинарь без well-known поиска); текущий свежий видит обоих. Слепые зоны закрыты: причина ошибки StageRunner теперь несёт диагноз «(движок видит: claude, codex / никого)» (`AdapterRegistry.knownCLIs`); в remediation-карточке — кнопка **«Повторить этап»** (chat с retryHint, prominent) рядом с «Перезапустить движок» — застрявший needsAttention-запуск оживляется одним кликом; план «не установлен» переписан: перезапуск+повтор первым шагом, ручной путь вторым, установка третьим.

25. **make run — одна копия приложения (правка автора 2026-07-31)**: `make run` открывал ДВЕ копии (install открывал /Applications, run — ещё и build/DerivedData → две иконки в Dock, два процесса). Теперь `run: daemon-install install` без второго open (install сам закрывает старую и открывает свежую из /Applications); плюс guard в `PetableApp.init`: свежий экземпляр terminate'ит другие процессы с тем же bundle id (forceTerminate через 3 с, автосейв делает это безопасным) — вторая иконка невозможна даже при ручном запуске из build/.

26. **Окно этапа: лог + чат + ссылки (правка автора 2026-07-31, референс Codex/Claude Code)**: `App/Sources/StageActivityView.swift` — клик по этапу в детали задачи открывает живую панель: шапка (этап, сотрудник·cli·модель, статус с ProgressView), **чипы-ссылки** из работы агента (URL-регэксп по логам/вердиктам + run.commitURL; `/pull/N` → «PR #N», `/commit/sha` → «коммит sha7»), **лог агента** (моноширинный, тёмная подложка, автоскролл, свернуть/развернуть 80/400 строк, дочитывание журнала таймером 2 с при running — «полный лог из файла», 6A), **переписка** (пузыри: человек справа-акцент, сотрудник слева; источники — chatMessage/needsInput/stageFinished-note), поле ввода по границам П9 (гейт/внимание — активно с Принять/Вернуть; running/merging — disabled с причиной; пройден — read-only), remediation-карточка внутри. Движок: `RunEvent.Kind.chatMessage`, `DaemonCore.chat` и `.reject` пишут сообщение человека в журнал ДО перезапуска этапа — переписка персистентна, переживает рестарты. Старый `stagePanel` остался только для незапущенной задачи. Тест `chatPersistsInJournal`.

27. **SCRUM-35: codex resume exit 2 + dnd + PR (правка автора 2026-07-31)**: разбор журнала — возврат по ревью РАБОТАЛ (changesRequested → work → исправлено), задача застряла на ПОВТОРНОМ ревью: `codex exec resume` не принимает `-s` (exit 2 «без вердикта», подтверждено live-пробой) — sandbox для resume теперь через `-c sandbox_mode=…`; stderr процесса собирается и хвост (300) включается в причину `.failed`; remediation-план «без вердикта». **Drag-and-drop**: Wire `moveRun`/`MoveRunCommand`, `Engine.move` (из {waitingGate, needsAttention}; путь усечь/дописать; merge → waitingGate; join и running игнор; счётчик возвратов НЕ трогается), токен `.draggable` + `.dropDestination` на узлах этапов, `controller.moveRun`. **Pull request**: `run.prURL`/`RunSummary.prURL`; `DaemonCore.ensurePullRequestIfReady` — на входе в merge-гейт (не experimental) push ветки (`WorktreeManager.pushBranchForPullRequest` + `githubOwnerRepo`) и `GitHubClient.ensurePullRequest` (идемпотентно: поиск по head; body с jira-маркером); нет токена/не GitHub — лог-событие, не блокер; ссылка — чипом в окне этапа (links: prURL первым) и в терминальной карточке. **Артефакты**: секция «АРТЕФАКТЫ ЭТАПА» в StageActivityView (коммиты sha + файлы artifact-событий). Тесты: `manualMove`, `ownerRepo`, codexResume расширен.

28. **Каталог CLI + экспорт/импорт сотрудника (правка автора 2026-07-31)**: `App/Sources/CLICatalog.swift` — справочник per-CLI (модели: claude `fable/opus/sonnet/haiku` + полные ID, codex `gpt-5.6-sol/codex/o3/o4-mini`; effort: обе CLI `low…max` — сверено с `claude --help` и `~/.codex/config.toml`; инструменты claude с описаниями + скоуп-паттерны `Bash(git *)`), `SearchableDropdown` (поле свободного ввода + поповер с поиском, имя+описание, «список справочный») и `SearchableMultiDropdown` (чекбоксы с описаниями, поиск, свой инструмент, выбранные-вне-каталога сверху). Подключено в `EmployeeEditor` (модель, effort, allowlist) и в настройки движка (модель по умолчанию per-CLI). Коммит из дропдаунов **явный** — значение приходит параметром `onCommit`, `EmployeeEditor.commit(_ override:)` применяет его поверх черновиков (не зависим от порядка обновления @State). `EmployeeTransfer` — экспорт настроек сотрудника в JSON (NSSavePanel, pretty+sortedKeys) и импорт (NSOpenPanel; id перегенерируется → новый сотрудник, конфликтов нет); кнопка «Экспорт JSON…» в шапке редактора, «Импорт из JSON…» в меню «Добавить сотрудника». Тест `employeeJSONRoundTrip`.

Проверка на текущий момент: **352 теста / 44 сьюта зелёные**, `make build` ок (демон в Resources, codesign валиден, TeamIdentifier=AL84K6U294 у приложения и демона). Демон переустановлен свежей сборкой, находит claude+codex.

## Следующее (в порядке)

1. ✅ LaunchAgent: `make daemon-install / daemon-status / daemon-uninstall` (Makefile) — бинарь в `~/Library/Application Support/Petable`, plist с MachServices, bootstrap; проверено: state=running, лог «слушаю». ✅ Установка из приложения + drain — пункт 14 выше.
2. ✅ Канвас — доделки 6A/2A/5A — пункт 15 выше.
3. ✅ Правки автора №5/№7/№8 — пункт 16 выше.
4. ✅ Слайс 10 остаток + дебаггер v1 (12) + тени (13) — пункт 17 выше. Остатки на будущее: детальный дизайн дебаггера отдельной сессией (TODOS: scrubber-таймлайн с зумом, синхронное сопоставление стадий в compare), трансляция skills во флаги CLI, fork с произвольного СОБЫТИЯ журнала (сейчас — с текущего этапа), материализация harness-файла в worktree через WorktreeManager на этапах (сейчас — append-to-system-prompt у claude).

## Правила, которые легко потерять

- Вердикт несёт ОТВЕТ сотрудника (JSON-блок), не события CLI; невалидный = failed.
- Возвраты: ревью+тесты+Reject — ОДИН счётчик, лимит 3 (`Engine.returnLimit`).
- Runtime НЕ в документе: только `RunSummary` идемпотентно по runID (П1′).
- Хранилище: `~/Library/Application Support/Petable` (T3), НЕ ~/Documents.
- Словарь UI: Принять/Вернуть, «возврат N из 3», «требует внимания», ≈$0.40.
- Коммиты — только по явной просьбе пользователя.
